# v8.7 follow-up UAT — issues raised after .env fix

After the `.env` was repointed to real prod (`fwlfiejvxmslkpoojggs`), end-to-end UAT surfaced this batch.

✅ Already validated by the .env fix: World Cup teams + schedules + Today's games on Home.

---

## Code changes shipped in this round

| # | Issue | Files | Fix |
|---|-------|-------|-----|
| 1 | RSVP shows "Going" on Home but "RSVP" on Discover | `components/WatchPartyCard.tsx`, `hooks/useData.ts` | New `useMyRsvps()` React Query cache shared across every card. Each card hydrates from this cache, so the same party renders the same label everywhere. |
| 2 | Profile → RSVP History blank | `app/rsvp-history.tsx` | Split nested PostgREST select into 2 flat queries (no schema-cache surprises). ALSO folds in parties created by the user as a fallback when the auto-RSVP path silently failed. Visible warn-logs on errors. |
| 3 | Fan Groups have no Join button | `components/GroupCard.tsx`, `app/(tabs)/discover.tsx` | New `joinable` prop on GroupCard. Wired on Discover → Trending Groups. Card now shows green "Join" → "✓ Joined". |
| 4 | Discover search filter only worked for Fan Groups | `app/(tabs)/discover.tsx` | `fetchWatchParties` now accepts and applies the search string against `title`, `venue_name`, and `venue_city`. |
| 5 | Soccer Cup → Watch Parties tab empty despite parties existing | `components/WCWatchParties.tsx`, `app/create-watch-party.tsx` | `useFocusEffect` so the tab refetches whenever it regains focus. Plus: when a Home-FAB party links to a Soccer Cup game, `event_id` is now also stamped on the row. |
| 6 | Clips chat-icon next to heart does nothing | `app/(tabs)/clips.tsx` | Tapping the icon now shows "Comments coming soon" alert (was a no-op handler before). |
| 7 | Clips auto-played on scroll | `app/(tabs)/clips.tsx` | Clips load paused on first view. After the user taps Play once in the session, autoplay re-engages for the rest of the session (matches expected TikTok/IG behaviour). |
| 8 | Venue distances measured from Dallas downtown for a McKinney user (33 mi shown for a 5-mi venue) | `app/create-watch-party.tsx`, `lib/venueSearchApi.ts`, `supabase/functions/venue-search/index.ts` | New opt-in **📍 Use my location for distances** pill on Step 1. Search center stays at home_city (broader catchment — McKinney users still see Plano/Frisco/Dallas venues), but distances on each card are computed from device GPS once the user grants permission. Hint text under results says which mode is active. Edge function redeploy required. |

---

## Outstanding — requires action OUTSIDE the codebase

### A. RSVP History — confirm whether RSVP rows actually exist

Paste this in the Supabase SQL Editor against `fwlfiejvxmslkpoojggs`:

```sql
SELECT
  r.id, r.watch_party_id, r.user_id, r.status, r.created_at,
  p.title, p.venue_name, p.venue_city, p.starts_at, p.rsvp_count,
  p.creator_id, (r.user_id = p.creator_id) AS is_creator
FROM watch_party_rsvps r
LEFT JOIN watch_parties p ON p.id = r.watch_party_id
WHERE r.user_id = (
  SELECT id FROM auth.users WHERE lower(email) = 'mustattie@gmail.com'
)
ORDER BY r.created_at DESC;
```

- **If rows come back** → the rsvp-history page should now show them on the next app reload (we split the query so schema-cache misses can't blank the page anymore).
- **If 0 rows** → the auto-RSVP path is failing. Re-run after creating a new party. If it stays empty, the `rsvp_to_watch_party` RPC needs another look (let me know and I'll dig deeper).

### C. Clips upload freeze on multi-upload

Not yet fixed — need to reproduce. Next session, please record exactly: which file picker (Camera vs Library), how many seconds the first clip is, and does it happen if you let the first upload fully complete before starting the second.

### D. "Following" tab on clips

The "Following" pill currently just re-orders all clips by created_at — it doesn't actually filter to clips from users you follow. I'll wire it to `user_follows` in the next pass; flagging here so you know it's intentional next.

---

## Test order in Expo Go

1. Scan QR → log in as test user.
2. **Home** → RSVP a party → should show "✓ Going" (unchanged behaviour).
3. **Discover** → same party should ALSO show "✓ Going" now (was "RSVP").
4. **Discover** → type a venue name in the search bar → Watch Parties list should filter (was Fan Groups only).
5. **Discover → Trending Groups** → each card now has a green "Join" button. Tap → "✓ Joined".
6. **Soccer Cup → Watch Parties** → leave the tab, create a Soccer Cup party from Home (linked to a WC game), come back → party appears.
7. **Profile → RSVP History** → previously RSVP'd / created parties show.
8. **Clips** → scroll to a clip → it does NOT auto-play. Tap the play icon → it plays. Scroll to the next clip → it auto-plays now (because you opted in).
9. **Clips → chat-bubble icon** → "Coming soon" alert appears.
10. **Create Watch Party → Step 1** → see the "📍 Use my location for distances" pill above the search bar. Tap it → system permission prompt → grant. Pill turns green "✓ Using your location". Type a venue name + Search → the "X mi away" numbers on each card should now reflect *your actual position* (e.g. a McKinney-to-Plano venue reads ~5 mi instead of ~33 mi). The hint line under the results says "Distances from your current location".

> **Deployment note:** the venue-search edge function was modified to accept `userLat`/`userLon`. Redeploy via Supabase Dashboard → Functions → venue-search → Deploy. Until redeployed, the function ignores the new fields and distances stay city-centred (no breakage).
