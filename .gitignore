# Claude Code local state
.claude/

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Editor
.vscode/
.idea/
*.swp

# Local asset-generation workspaces (intermediate SVG/HTML, screencaps)
/tmp-icon-gen/
/tmp-screencaps/

# Maestro QA screenshot captures (per-run output, not source)
/screenshots/

# Build artifacts (EAS keeps them remotely; too large for git)
*.aab
*.apk

# iOS signing secrets — App Store Connect API Key
# Stored locally only, referenced by eas.json submit profile
**/AuthKey*.p8
fan-wave-app/AuthKey.p8

# Apple submission video evidence — already uploaded to ASC, too large for git
Delete_Account_Demo.mp4
*.MP4

# Local APK/IPA artifacts converted by the build watcher — not source
builds/
